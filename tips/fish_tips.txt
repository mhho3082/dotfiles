fish_update_completions - Update auto-complete

fish_vi_key_bindings      - vi mode
fish_default_key_bindings - back to default

alt + <- - previous directory
alt + -> - previous directory

alt + w  - what the typed command does
alt + s  - prefix sudo
